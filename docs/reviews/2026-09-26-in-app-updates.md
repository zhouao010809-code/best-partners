# 应用内更新验收 — 0.1.1

## 已观察

- 设置页复用“应用更新”。GitHub Release 检查后可直接下载 DMG，显示实际进度；取消、SHA-256 校验失败与重试均可恢复。
- 未签名预览版下载完成后显示“打开安装包”，由系统打开 DMG，用户退出旧 App 后拖入 Applications 完成替换；界面不宣称已自动安装。
- 离开设置页再回来仍能看到下载状态。只有主进程已检查的官方资产可下载；下载和安装 IPC 不接收任意 URL 或路径。
- 应用仅清理更新器自己在 `userData/updates` 创建的旧下载目录，避免每次重启遗留完整 DMG；不操作资料、密钥和数据库目录。
- 本地构建的 `0.1.1` 已正常启动，设置页显示版本 0.1.1，原大脑连接和已保存的模型配置仍可读取；没有发起真实模型请求。

## 验证证据

- `npm run verify:fast`：架构、全部类型检查通过；1,083 单元 + 626 集成 + 23 MCP + 13 company MCP，共 1,745 项通过。
- 干净源码快照中的更新及设置布局组件：23 项通过。
- 隔离 Electron：10 项通过，包括开发/打包更新检查、两模式损坏文件拒绝后重试、取消下载、首次使用 4 场景，以及打包版项目问问连接恢复。
- 另对两模式的下载用例增加 720×800 按钮可见性及横向溢出断言，2 项复跑通过；保存宽屏和紧凑截图。
- 模拟安装器传输使用固定合成字节，真实执行下载、流式写入和摘要校验；`shell.openPath` 用替身，未在测试中安装应用。原始资料、用户配置及配置哨兵字节保持不变。
- `hdiutil verify` 验证真实 DMG；只读挂载后核对 Applications 链接、3 处版本号一致、Electron/server 构建文件哈希一致。DMG SHA-256：`a9aabdb4ee75af35c89c7b14fc5c92d134ca4c679799dca7dca63f741cd11749`。

## 设计推断与未验证

- 已接入 Electron 原生自动更新适配器、签名能力判断、下载完成后正常退出/关闭服务再安装的控制逻辑，以及正式资产生成器。
- 当前发布者没有 Apple Developer 账号，本机没有可用的 Developer ID 身份。正式模式会阻断缺签名或缺公证的构建；本次只发布手动安装预览 DMG，不发布自动更新清单。
- 两个真实签名版本之间的安装、失败恢复、钥匙串连续性和自动重启未验证。原生更新状态单测使用事件替身，不能替代系统级验收。
- 尚无真人首次使用研究；自动化通过仅证明所覆盖的流程。应用内真实 GitHub 检查与发布资产核对在交付时执行，实际客户端下载速度取决于网络。

本次包从 Git HEAD 加本次文件变更构建，没有混入工作树已有的首页、收藏插件和设置布局未提交改动。
