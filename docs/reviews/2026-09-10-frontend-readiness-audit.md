# 前端产品就绪审查

日期：2026-09-10。范围：当前工作树，包括全部未提交及未跟踪前端代码。真实界面和独立应用的验收由主审负责；本审查没有操作真实大脑、浏览器或模型。旧缺陷已授权修复，新增能力依据已批准的 [问问产品方案](../superpowers/specs/2026-09-10-assistant-product-readiness-design.md) 实施。

## 高置信缺陷及处理

### P2：较早的历史对话不可达（已修复）

修复前，服务端只返回最近 100 条对话，面板搜索只在这份列表中过滤。第 101 条较早对话即使仍然保存在数据库、可按 ID 读取，也没有可用入口。用内存 SQLite 填入 101 条合成对话即可复现，不涉及真实用户资料或模型请求。

修复采用服务端标题及关联资料路径搜索、稳定的 `updated_at + id` 游标分页，每次最多 100 条；返回元数据而不携带消息正文。面板搜索防抖并取消旧请求，加载更多失败保留已取得的列表并允许重试。空查询仍支持最近历史和进行中任务恢复。

定位：[history.ts:18](<PROJECT_ROOT/src/server/assistant/history.ts:18>)、[Panel 历史加载:145](<PROJECT_ROOT/src/client/components/assistant/AssistantPanel.tsx:145>)。对应文件：`src/server/assistant/history.ts`、`src/server/assistant/service.ts` 的 `list`、`src/server/api/routes/assistant.ts`、`src/shared/api/assistant.ts`、`src/client/api/client.ts`、`src/client/components/assistant/AssistantPanel.tsx` 的 `loadHistory`。

回归：`tests/integration/assistant-history.test.ts` 覆盖 101 条、相同时间戳、字面特殊字符、跨查询及损坏游标；`tests/component/assistant-panel.test.tsx` 覆盖搜索竞态和更多加载失败恢复。

### P2：停止并新建后残留上一个任务的断连错误（已修复）

修复前，轮询失败的 `pollError` 只有新一轮运行时才清空。任务停止成功后新建空对话，仍显示“进度连接中断，任务可能仍在运行”，错误归属与实际任务不符。隔离组件用例复现了“发送运行中任务 → GET 失败 → stop 成功 → 新对话”。

修复将错误绑定到 conversation ID，只有对应任务仍运行时显示；已确认的停止/完成及新建会清空错误，迟到轮询还须通过当前 ID、状态与 AbortSignal 检查。定位：[AssistantPanel.tsx:232](<PROJECT_ROOT/src/client/components/assistant/AssistantPanel.tsx:232>)；修复涉及 `receive`、轮询 effect、`startNewConversation`。新回归包含真实可见文字入口与停止后的错误清理。

### 原有能力边界：问问未发送草稿无法跨重启恢复（本轮已补持久链路）

原先 Panel 只保存在 React state；收起再打开保留，但卸载或刷新即丢失。Electron 使用临时 partition 和随机本机 origin，单纯补 localStorage 也无法兑现重启续接。本项最初属于新需求缺口，用户批准持久化后纳入交付要求。

新增独立 `assistant_drafts` 数据库表、带 revision 的保存与删除、最后活动草稿恢复，不依赖浏览器 origin。客户端串行保存，切换前 flush；较晚的旧请求不能覆盖新文字；服务端接受相同内容的未知结果重试，冲突时保留输入并提供另存；明确清空保留版本墓碑，延迟请求不能复活已清空内容。旧会话和未发送草稿均可从历史区找回。

对应：`src/server/assistant/draft-service.ts`、`src/server/api/routes/assistant-drafts.ts`、`src/shared/api/assistant-drafts.ts`、`src/server/db/migrations/014_assistant_drafts.sql`、`src/client/components/assistant/useAssistantDrafts.ts`。

## 新能力实际验收补修

主审在隔离打包应用的 CUA 验收中发现：聊天归档已经产生成功回执并写入资料，输入区附件条仍显示“临时附件”。原因是 `ready` 附件停止轮询后，Panel 没有把任务/归档回执变化传给 Picker 的刷新入口。

已以对话 ID、任务状态及所选附件的归档回执生成稳定刷新标识，任务结束或真实归档回执变化时重新读取所选附件。普通输出文字、更新时间和相同回执不会触发重复 GET。组件先红测 2/2，再修复；覆盖“运行中收到归档回执”“无卡片时任务结束”两种刷新触发及文字变化不刷新。受影响 Panel/就绪测试 40/40 通过，客户端 TypeScript 检查通过；加此前 API 7 项，当前前端目标集合共 47 项。

## 覆盖及未误报事项

静态覆盖路由、全局导航/搜索、首页、收件箱、资料库、知识页、队列/独立提炼、候选审阅、操作恢复、回收站、设置连接及问问。检查重点是异步请求归属、被卸载页面的状态写回、深链/刷新、草稿恢复、运行/停止、错误重试、IME Enter、Escape、来源/对话作用域、模型不可用状态及实际下一步链接。

本轮改动前运行全部组件测试，33 个文件、533 个用例通过。结合已有候选、目录、恢复和来源边界回归，未发现其他有充分证据的新 P1/P2。没有把布局偏好、尚未承诺的收件箱跨页表单保存、缺少 OCR 或未开放的跨会话并发当成旧产品 bug。测试全通过也不等于已经验证真实模型质量或新版打包应用。

## 与导入 → 理解 → 整理 → 复用相关的产品补齐

| 优先级 | 观察与分类 | 本轮落地/建议 |
| --- | --- | --- |
| P1 | **全新后端能力**：原来没有聊天上传、PDF 文本读取和附件账本。 | 统一 PDF/MD/TXT 导入服务；临时原件、读取状态、页码范围、归档回执分别展示。扫描/密码文件提示真实限制，保留原件，不能冒充已完成 OCR。问问及收件箱复用 `AttachmentPicker`。 |
| P1 | **现有入口隐藏**：新对话原本只有加号；草稿又不持久，用户难以放心开始新话题。 | 可见“新对话”文字、历史搜索全部记录、保存旧草稿，空白新对话不携带旧文件或固定来源。第一阶段继续单个运行任务。 |
| P1 | **全新数据能力**：原来没有 usage/capacity/context 回执，无法诚实显示百分比。 | 上下文圈显示最后完成模型步骤的实际输入加输出/已核验容量，不使用多步骤累计。展开区分历史限制、已选附件、实际读取/检索线索、输出预留和累计消耗。容量未知无百分比，输入改变时明确上一请求。 |
| P2 | **已有工具链的流程补齐**：提炼与人工审阅已有，但聊天文件尚未接入来源归档。 | 用户明确归档/提炼后返回真实资料位置与任务卡；可读失败的原件仍能保留。候选结果与正式已入库状态分开，带页码/范围的来源可下载核对。 |
| P2 | **新增前端续接**：最近 24 条历史与单对话 100 条上限未被解释。 | 显示实际选择/省略的历史及消息额度；“带摘要继续”先给可编辑摘录，显式选择要带入的文件，放入新草稿后由用户发送。暂不把自动摘要说成模型已确认结论。 |

## 验证记录

- 历史 bug 红测：服务端 3/3 失败、组件 3/3 失败；修复后原目标组件 27/27、历史/服务/API 16/16 通过。
- 新增草稿服务及当前历史/API：历史/API 2 个集成文件、6 个用例通过；新增草稿路由/服务 4 个用例通过。覆盖浏览器 origin 无关的恢复、网络重试、旧版本冲突、删除后延迟请求。
- 新前端特性补充覆盖串行保存与重挂载、保存失败阻止切换、当前草稿旧快照、带文件的旧对话恢复/新建/历史找回、上传与页码、取消/移除区分、同会话新上传批次、保存失败不上传、真实用量与 unknown、摘要编辑、归档回执与附件引用。目标组件为 Panel 24 项、API 7 项、新就绪回归 14 项，合计 45 项均已分别通过；最后补充的历史附件 fallback 红测 1/1 失败、修复后就绪文件 14/14 通过，已有空草稿仍尊重主动移除；客户端 TypeScript 检查通过。补充修复了仅附件范围恢复与恢复对话期间外部提问的竞态。最终全套检查与构建由主审统一运行。
- 没有启动或停止真实服务，没有 Vite 清空 dist，没有运行真实模型请求。真实桌面、小屏布局、新应用冷启动及附件端到端仍需主审最后验收。
