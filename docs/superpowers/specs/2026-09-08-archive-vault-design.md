# 档案库：封存柜（用户已确认）

## 已确认内部修订

2026-09-08 用户已确认开柜草图：`generated_images/01a05359-1c54-70a1-93f2-4763b094e998/exec-a7e0fed1-d498-4ddf-90df-e5792b34c08d.png`。

开柜后保留可见柜框、两侧柜门、暗色背板和连续隔层。分类采用上一版有银色反光、轻微侧倾、可见纸张层次的立体文件夹，统一尺寸；桌面四列、较窄两列、手机单列。替代下文的“闭合档案盒”内部方案。柜体以深色为主，银色通过反光和边缘呈现。截图内静态装饰与数字不作为数据源，真实分类、数量、目录、状态继续取自现有 API。默认关柜、开合状态、搜索和原文调阅规则不变。

## 设计依据

档案用于封存，需要调阅才打开；知识库才是可直接翻阅的书柜。用户否定了开放柜格、裸露书盒以及大面积银白涂装。此前两张效果图均不作为实施依据。

柜体保持深炭灰，银色只用于细边、锁扣及柔和反光，不使用粗糙铁纹、铆钉、棕色材质或整面银漆。

## 交互

### 已确认连续性修订

柜壳的位置、宽高、柜框材质在四个开合阶段保持不变。柜外固定区域承载面包屑、搜索、分类与状态筛选；柜内为固定大小的独立滚动区。首次关柜不挂载资料页、不读资料；用户点击后即挂载准备内容，由柜门逐渐揭示，开门动画完成才允许操作。合柜期间保留内容在门后但禁用交互，完全合拢后卸载。此规则替代下述“开柜后才挂载”的旧时序，深链接与筛选数据契约不变。

- 进入 /library 时柜门关闭，包括带目录或文件链接的首次进入。不读取档案列表或原文。
- 点击锁扣，锁芯转动，两扇门沿铰链打开，随后显示真实分类档案盒。
- 分类、搜索、筛选、分页沿用原流程；点击文件才打开原文。深链接在开柜后恢复目标目录与文件。
- “封存并合柜”关闭调阅区，清除文件选择，保留目录和筛选。离开页面再进入恢复关柜。
- 开柜后聚焦调阅标题，合柜后聚焦锁扣；减少动态效果时跳过动画。
- 锁扣只是调阅交互，不代表密码或加密保护。

## 数据边界

导航、页面标题与回收站来源名称统一为“档案库”。URL /library、origin library、API、YAML、01图书馆 路径及真实文件不变。文件内容中的“原始资料”语义保留。永久删除仍只在统一回收站。

## 实现与验收

ArchiveVault.tsx 管理开合状态；现有 LibraryMaterials 在开柜后挂载；CollectionFolder.tsx 使用闭合档案盒，真实分类和数量不造假。新增样式限制在档案页。

相关组件测试覆盖关柜读取边界、深链接、筛选、版本、分页、阅读、回收站和导航。浏览器只读检查开合、目录与原文、键盘及窄屏。部署仅替换客户端并校验 server/electron/native 不变。
