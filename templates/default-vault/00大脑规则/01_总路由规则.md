# 01 总路由规则

先判断用户要处理的是资料、知识还是输出，再进入对应区域：

1. 新收到的网页、文字或文件进入 `01图书馆`，原文保留。
2. 要整理可复用的方法、判断或事实时，先生成候选预览；候选不等于正式知识，确认入库后才进入 `02知识库`。
3. 要写文章、课程或分享时，优先调用已确认的 `02知识库`，草稿放入 `03大讲堂`。
4. 要发送、同步或发布时，先展示最终预览，确认后发送。

每一步都保留来源链接或来源文件名，让来源可回看。无法判断区域或缺少来源时，不自动写入正式知识。
